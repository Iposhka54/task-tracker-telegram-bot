# task-tracker-telegram-bot

## Environment variables

- `BOT_TOKEN` — required Telegram bot token
- `LOG_LEVEL` — optional log level for zap logger (`debug`, `info`, `warn`, `error`, etc.), default `info`.